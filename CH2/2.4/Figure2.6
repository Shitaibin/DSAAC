int 
MaxSubSequenceSum( const int A[], int N )
{	
	int ThisSUm, MaxSum, i, j, k;

	for ( i = 0; i < N; i++ )
	{
		ThisSum = 0;
		for ( j = i; j < N; j++ )
		{
			ThisSum += A[j];

			if ( ThisSum > MaxSum )
				MaxSum = ThisSum;
		}
	}
	
	return MaxSum;
}

