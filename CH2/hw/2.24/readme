test.c is the correct algorithm.
changed.c which is the file has been replaced the sentence.

start up shell `testshell.sh` will give you the result.
this shell contains compiling those two source files. and then
put out the answer. the input data file is `in`.


the answer of question 2.24.
----------------------------

	It does not work. Because, when there is only two elements,
	Center equals to Left, so `Center -1 < Left`, you konw it 
	will cause the segementation fault.
